const Navbar = () => {
  return (
    <section>
      <p className="text-xs underline hover:cursor-pointer hover:text-primary font-jetbrains-mono  text-end absolute top-3 right-4 z-50">
        extended version 2.0
      </p>
    </section>
  );
};

export default Navbar;
